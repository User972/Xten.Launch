# Normalize line endings. Files consumed on the Linux VM (shell scripts, compose, nginx/sql
# config) MUST be LF regardless of the committer's OS or core.autocrlf setting.
* text=auto
*.sh   text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf
*.conf text eol=lf
*.sql  text eol=lf
